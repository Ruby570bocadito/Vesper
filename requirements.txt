# Vesper — Python bridge dependencies
# Install: pip install -r requirements.txt
#
# Kept intentionally lean: only what the bridge imports at runtime.
# Optional extras live in requirements-optional.txt.

# Bridge gRPC transport (modules/bridge/bridge_grpc.py)
grpcio>=1.64.0
protobuf>=6.33.0

# Testing (modules/bridge/tests)
pytest>=8.0
